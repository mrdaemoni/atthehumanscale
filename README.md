# At the Human Scale

A bilingual book-in-progress from inside a real personal AI partnership, about keeping life at human scale.

## Development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```

The site is deployed to GitHub Pages from `main` with the workflow in `.github/workflows/deploy.yml`.
